# mario 0.4.3：邮箱确认链路修复

日期：2026-09-09。

## 修复

- 云端 Site URL 原为 `http://localhost:3000`，确认邮件会跳转到未运行的服务。现已配置为 `https://lokinko.github.io/mario/auth/`，同步加入重定向白名单，保留邮箱确认要求。
- 新增 GitHub Pages 公开回调页面：向账户服务确认邮箱状态后显示成功；密码恢复提供新密码表单；过期、已使用、缺少凭证和网络失败均提供提示。
- 兼容 Supabase 开启邮箱确认时注册接口返回直接用户对象的格式，避免账户已创建但客户端误报缺少用户信息。
- 保留登录失败后的注册、忘记密码及重发确认邮件入口；密码恢复支持应用内原始链接/验证码和浏览器回调页面。

## 验证结果

- `npm test`：前端 TypeScript/Vite 构建，92 项服务端测试，4 项回调页面测试，2 项桌面测试，本地 API 鉴权冒烟全部通过。服务端覆盖投资规划、持仓事件、归因、研究证据、复盘规则、记忆、AI 工作流与数据授权等已有模块。
- 服务端、桌面端 `cargo clippy --all-targets -- -D warnings` 通过。
- 真实 Supabase 临时账户测试：未确认账户 → 邮件确认 → 公开回跳 → 获取已确认用户；恢复链接 → 更新密码；重复链接被拒绝。没有代替真实用户确认邮箱。
- 真实密码恢复测试：原始邮件链接和 OTP 均可验证；更新后旧密码被拒绝、新密码可登录。
- 真实云同步测试：设备 A 加密上传 → 设备 B 导入恢复密钥并拉取；云端不含明文标记；临时用户及同步数据清理通过。
- 公开页面已部署并通过浏览器读取验证；无凭证访问不会假报成功。
- macOS arm64 DMG、Android arm64 调试 APK 构建通过；发布包预置公开云配置，已有用户配置优先。

## 边界

上述为本次自动回归及真实认证/同步链路检查，并不表示所有第三方行情源、用户自配 AI 模型或所有实机环境均已逐一测试。Android 包仍为调试签名，macOS 包为临时签名且未公证。

默认免费邮件服务不允许定制模板，因此此次只更新回调路由，保留默认邮件模板。仓库中的中文模板供未来自定义 SMTP 使用。邮件服务接受发送请求不等于收件箱已收到；真实用户需要打开最新邮件亲自确认。GitHub Pages 在不同网络的可达性可能不同。
