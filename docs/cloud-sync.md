# 账户与端到端加密云同步

## 产品边界

账户用于识别用户与绑定设备，不改变 mario 的“本地优先”模式。登录后默认开启自动同步，可在“账户与同步”关闭；该开关只保存在本机。日常保存不需要再点击上传或拉取。应用关闭后不运行同步任务，尚未同步的本地数据会在下次打开时重新检查。

云端同步以下投资域数据：财务档案、目标、持仓、决策与决策复盘、决策内冻结的规则检查、AI 分析历史、周期复盘、投资规则及修订历史、研究证据、组合检查点及其冻结持仓，以及不可变组合流水。

以下数据永不进入同步包：

- 模型 API Key；
- 行情 API Key；
- 模型提供商、模型名称和 Base URL；
- 云端 Access Token 与 Refresh Token；
- 云端地址与 Publishable Key；
- 端到端加密恢复密钥。
- 本机复盘提醒开关与去重状态。

模型密钥、行情密钥、账户令牌、同步恢复密钥使用不同的系统钥匙串条目保存；恢复密钥还按账户 ID 隔离。用户密码只从本地服务直接发送给账户提供商，不落盘。

## 低开销自动同步

- 成功保存收支、持仓、存款、研究记录或问答后，等待 1.5 秒合并连续操作；持续修改时最多合并 10 秒。同步本身不会再次触发保存事件。
- 打开应用、回到前台、网络恢复时检查；焦点事件至少相隔 10 秒才启动检查。页面隐藏或窗口失焦时停止定时请求。前台闲置检查间隔由 1 分钟逐步延长到 5 分钟。
- 本机内容与基线一致时，仅查询云端 `revision,content_hash` 两列。版本与指纹均未变化就结束：不下载密文、不加解密、不更新 SQLite 基线、不写云端。
- 有变化时使用现有双向合并和条件写入；没有额外推送服务、WebSocket、轮询数据库表或消息队列。云端仍是一张加密快照表，不需要新增迁移。
- 网络失败按 10、20、40 秒等指数退避，上限 5 分钟；登录失效、缺失恢复密钥和记录冲突暂停自动重试，页面提示处理。版本条件写入发生竞争时只自动重新合并一次。
- SQLite 数据及持久同步基线就是待同步状态，进程退出不会丢失待上传修改。本机写入与同步共用串行锁；自动任务在本机操作忙时让行，已有同步请求完成后再处理后续写入，避免请求重叠导致数据覆盖。
- 前端自动刷新已合并的数据；若正在编辑表单，会保留输入并提示查看更新，在核对前阻止旧表单覆盖新数据。问答输入与对话不因同步刷新而重置。
- 复用 HTTP 连接与未临近过期的访问令牌，避免每次检查都请求登录刷新。访问令牌的签名与账户权限仍由云端验证。参考 [Supabase 会话说明](https://supabase.com/docs/guides/auth/sessions)。退出使用 `scope=local`，只退出本设备，参见 [官方退出范围说明](https://supabase.com/docs/guides/auth/signout)。

新设备仍须登录同一账户，并从原设备导入恢复密钥一次；之后无需手动同步。这里选择的是低复杂度、最终一致的同步：不是秒级实时广播；有变化时仍传输完整加密快照，不能称为网络逐记录增量。适合当前个人档案规模，避免为很少的数据变动维护复杂分片协议。

本地接口：`POST /api/cloud/sync/auto`（返回 synced / busy / disabled / signed_out），`PUT /api/cloud/sync/settings`（`{ "enabled": true }`）。旧上传、拉取和显式恢复接口继续保留在折叠的手动操作中。

## 同步协议

1. 本地数据库按固定白名单导出带类型的数据表快照。
2. 对快照内容计算 SHA-256，导出时间不参与内容指纹。
3. 使用随机生成的 256 位恢复密钥和 XChaCha20-Poly1305 加密；固定协议版本作为附加认证数据。
4. 云端只保存密文、随机数、内容指纹、结构版本和单调递增的 revision。
5. 上传通过数据库函数对 `expected_revision` 做原子比较并写入。自动同步遇到版本竞争时重新读取合并一次，持续竞争或记录冲突则停止。
6. 拉取先完成认证解密、结构白名单、记录数量、字段长度、数字有效性和内容指纹检查，再基于上次同步版本合并本机与云端变更；只在单个 SQLite 事务中新增、更新或删除发生变化的记录，同时保存同步基线。

当前同步数据结构为 v9：v6 为持仓与组合流水加入汇率来源和观察日期，v7 增加不可变流水冲正关联，v8 增加用户管理的长期记忆偏好与注释，v9 增加持仓证券价格估值证据。客户端能够读取和恢复 v1–v8 数据包：v1 不包含组合检查点；v1/v2 持仓没有估值日期和折算汇率；v1–v3 不包含组合流水；v4 流水没有导入标识，恢复时按手工记录处理；v5 的汇率没有来源和观察日期；v6 没有冲正关联；v7 没有长期记忆偏好；v8 没有证券价格估值证据。恢复旧包后，缺失字段会在本机安全降级为用户声明市值。新版客户端下一次成功上传会以新的 revision 写入 v9 包。密文外层版本与解密后数据版本不一致时，恢复会被拒绝。

恢复密钥没有上传到云端。新设备必须从另一台设备导出并手动导入；密钥丢失后，服务运营方也无法恢复密文。改名前生成的 `zhiheng-sync-v1:` 恢复密钥与旧加密包仍可读取，导入或再次上传后会规范化为 `mario-sync-v1:`，避免品牌迁移造成数据不可恢复。

## 冲突策略

当前使用按记录的三方合并，不使用“最后写入者获胜”：

- 比较上次同步基线、本机当前记录和云端当前记录。稳定主键标识对象，规则修订使用 `(rule_id, revision)` 复合键。
- 仅一端修改或删除的记录自动采用该变更；双方新增不同记录自动合并；内容相同的修改自动收敛，单独的 `updated_at` 差异不算内容冲突。
- 同一记录被双方改成不同内容，或一端删除另一端修改，停止整次合并并列出冲突记录。不会把金额、数量、估值依据等关联字段逐字段拼接成用户从未确认过的记录。可在两端核对并统一记录后重试。
- 上传前读取云端最新版本、合并并校验，再以该 revision 做原子条件写入；如果另一个设备抢先上传，仍拒绝覆盖，重试会重新读取并合并。
- 拉取默认合并，保留本机未上传的修改。拉取后的同步基线是云端版本，不能错误地记为本机合并版本，否则后续上传会丢失本机变化。
- 本机对投资域写入和账户/同步操作串行处理，避免网络请求期间切换账户、变更令牌或修改被导入的数据。
- 基线快照保存在本机，按云端项目 URL 和用户 ID 隔离，退出登录不删除持久基线；重新登录同一账户可继续合并。本地数据记录所属账户/项目，阻止换号后误上传给另一账户。
- 旧客户端只有内容 hash，只有本机仍与旧 hash 一致时才能重建旧基线；否则不猜测删除，只合并无冲突的新增记录。新设备首次导入若有相同主键的不同本机记录，会要求核对。
- 高级“用云端版本恢复本机”仍可明确确认替换；覆盖前在本机 SQLite settings 的 `cloud.pre_restore_backup.<随机ID>` 留存快照，每次恢复单独保存。该备份与各账户基线均不上传。

“增量”指记录级合并和本地增量写入。云端仍传输完整的端到端加密快照，并非网络按行上传；无需部署新的云端表结构，也不会让服务端接触明文。未变化的上传跳过写入。模型配置、模型/行情密钥、账户配置和本机提醒设置均不参与合并。

## Supabase 参考部署

1. 创建 Supabase 项目，按产品需要配置邮件确认和自定义 SMTP。
2. 执行 [`server/migrations/supabase-cloud-sync.sql`](../server/migrations/supabase-cloud-sync.sql)。
3. 客户端“账户与同步”页面填写项目 URL 与 Publishable/anon Key。
4. 创建账户、完成邮件确认并登录。
5. 首次上传后立即导出恢复密钥，离线保管。

若确认邮件未收到或已过期，可在“账户与同步”页面重新发送。确认链接由 Supabase 托管，验证后跳转到 https://lokinko.github.io/mario/auth/，页面向账户服务核实邮箱已确认后才显示成功。然后返回 mario 使用原密码登录。过期或已使用链接显示重新发送提示，不应跳转到 localhost。

公开回调页面位于 `site/auth/`，由 `.github/workflows/auth-pages.yml` 发布到 GitHub Pages。更换部署时需要同步修改页面的公开项目配置及 CSP。先部署，再运行 `bash scripts/configure-auth-callback.sh <project-ref> https://lokinko.github.io/mario/auth/` 配置 Site URL、追加重定向白名单；脚本保留原白名单和邮件模板，并将原配置备份到本机临时文件。管理令牌仅用于部署，不进入客户端或仓库。当前免费默认邮件服务不允许修改模板，因此使用默认邮件；仓库内中文模板仅供未来自定义 SMTP 部署使用。

页面立即清除地址栏中的凭证，只在内存中保留必要令牌，不写浏览器存储、不引入第三方脚本。直接打开回调地址不会显示验证成功。运行 `node --test scripts/test-auth-page.mjs` 检查页面状态；运行 `node scripts/test-email-confirmation.mjs <project-ref>` 使用临时账户验证真实确认、回跳、密码更新及链接复用拒绝，测试不发送邮件并清理临时账户。

### 登录提示与忘记密码

登录失败后，就近显示错误并提供“去注册”“忘记密码”和“重发确认邮件”。Supabase 通常将不存在的邮箱与错误密码统一为 `invalid_credentials`；应用因此提示“邮箱或密码不正确；尚未注册请先注册，忘记密码可重置”，不把失败猜测为未注册。若提供商明确返回 `user_not_found`，则提示先注册。

忘记密码支持两种流程：打开最新重置邮件的链接，在公开页面验证身份后输入并确认新密码；或在 mario 中复制邮件按钮的完整原始链接，验证后设置新密码。已打开的链接可能已消耗，应重新发送。应用也支持验证码，但当前默认邮件模板只有链接；仓库内可选中文 recovery 模板包含 `{{ .Token }}`，需自定义 SMTP 等支持模板修改的配置。完成后返回 mario 登录。

服务端只向已配置项目的固定 `/auth/v1/verify` 发送 `type=recovery` 验证请求；拒绝其他项目、注册确认链接和歧义参数。验证后的访问令牌仅保留在服务端内存，客户端只得到随机重置凭证，10 分钟后或应用重启后失效。密码规则失败可重试，成功后凭证立即作废。密码重置不会替换投资同步恢复密钥，也不自动登录。

接口：`POST /api/cloud/password/recover`、`/verify`、`/reset`（后三者均使用 `/api/cloud/password` 前缀）。发送接口不泄露邮箱是否存在。真实认证验证可运行 `bash scripts/test-password-reset.sh <project-ref>`；测试仅创建临时账户，通过管理员生成测试邮件凭证后走公开验证接口，验证链接/验证码、新旧密码登录，并清理测试账户，不发送真实邮件或改写日常钥匙串。

数据库表启用 RLS；`anon` 无表权限，`authenticated` 只有按 `auth.uid()` 读取自己的密文权限。写入只能通过带 revision 比较的安全函数执行。生产环境还应配置备份、审计、速率限制、邮件投递监控和密钥轮换流程。

应用默认内置 mario 的公开账户服务配置（`client/src/lib/cloudConfig.ts`），首次进入账户页面时自动保存，无需用户填写服务地址。自建部署可以在构建进程中成对设置 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_PUBLISHABLE_KEY`；只提供一项不会与默认服务混用，页面会提示补全配置。已有用户配置优先，不会被构建值覆盖。公开回调页面的项目配置也需随自建部署更新。Publishable Key 本来就会随客户端分发，真正的安全边界必须由 RLS 和数据库函数建立；不要以任何形式把 Secret 或 `service_role` Key 放进客户端构建。

也可以使用官方 Supabase CLI 部署并执行真实的双设备加密同步测试：

```bash
supabase login --name mario
supabase db query --linked --project-ref <project-ref> \
  --file server/migrations/supabase-cloud-sync.sql
cargo build --manifest-path server/Cargo.toml
npm run supabase:test -- <project-ref>
```

测试会创建一个不发送邮件的临时已确认用户：设备 A 写入带唯一标记的数据并上传，设备 B 导入恢复密钥后拉取；随后检查云端密文不包含明文标记，并删除临时用户、级联密文和本机测试凭据。脚本只把敏感 Key 保存在权限为 `0600` 的临时文件和进程内存中，不打印或写入仓库。日常客户端只配置 Publishable Key，绝不使用 `service_role`/Secret Key。

## 可替换边界

服务端的账户与对象存储调用通过 `CloudSyncProvider` 抽象。Supabase 是首个实现，自托管服务只需实现注册、登录、令牌刷新、读取密文和原子版本写入，不需要接触明文投资数据。
