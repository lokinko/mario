# 邮箱确认访问问题复查

日期：2026-09-09。

## 当前检查结果

- 直连 `https://lokinko.github.io/mario/auth/` 返回 HTTP 200；认证服务的公开设置接口也返回 HTTP 200。
- 当前运行环境通过默认代理访问回调页时，在 20 秒后超时。这只能说明本机该网络路径异常，不能据此断定用户浏览器的失败原因。
- 回调页 4 项自动化测试通过。
- 使用临时账号检查注册确认、公开回跳、读取已确认用户、密码恢复和已使用链接拒绝。最终一轮全部通过，临时账号已删除；此前两轮在密码恢复的重复链接检查阶段失败，因此不能宣称链路在所有网络条件下稳定。
- 为真实认证检查脚本补充阶段和非敏感状态诊断，不输出邮件链接、验证码、密码或令牌。

## 尚待确认

需要用户提供失败页面截图，或地址栏的域名和路径（移除查询参数和片段），判断是否为旧邮件中的 localhost 跳转、GitHub Pages 网络不可达、认证服务不可达，或链接已使用/过期。

原先 `http://localhost:3000` 的回调配置问题已在 0.4.3 修复，见 `release-0.4.3.md`。本次没有再次修改云端配置，没有替真实用户确认邮箱，也没有重新发送真实用户邮件。验证邮件中的凭证不应写入问题记录或代码库。
